# Local Development Environment Variables
# These are the default values when no environment file is specified

# Image names (will use build context when not specified)
UI_IMAGE=freqtrade-ui:latest
TRADER_IMAGE=freqtrade-trader:latest

# Ports for local development
UI_PORT=8080
PORT_ICHIV1=8081
PORT_LOOKAHEAD=8082
PORT_MACD=8083
PORT_CUSTOMSTOPLOSS=8084
PORT_MACDCCI=8085

# Local user data path
USER_DATA_PATH=./user_data 